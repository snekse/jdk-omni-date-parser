# Signing and Maven Central credentials are injected in CI via ORG_GRADLE_PROJECT_* environment variables.
# For local signed publishing, pass them on the command line:
#   -PsigningInMemoryKey=... -PsigningInMemoryKeyPassword=...
#   -PmavenCentralUsername=... -PmavenCentralPassword=...
